<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" type= "text/css" href ="/static/css/datatables.min.css"/>
    <link rel="stylesheet" type= "text/css" href ="/static/css/bootstrap.css"/>
    <title>Document</title>
</head>
<body>
    <div class="container">
        <div class="row">
            <div class="col">
                <table id="dtable" class =" display" style="width: 100%"> 
                    <thead>
                        <tr>
                            <th>Name</th>
                            <th>Joined</th>
                            <th>Assigned</th>
                            <th>Change Status</th>
                            <th>Delete</th>
                        </tr>
                    </thead>
                    <tbody>
                        <%if(result.length != 0){
                            result.forEach((a)=>{
                                var date = new Date(a.joined)%>
                                <tr>
                                    <td><a href=<%='/admin/'+adminId+'/viewprofile/'+a.userId %> class="text-decoration-none"><%=a.name%></a></td>
                                    <td><%=date.toLocaleDateString()%></td>
                                    <td><%=a.assigned%></td>
                                    <td><a href=<%='/admin/'+adminId+'/changeStatus/'+a.id+'/'+a.name %>>Change</a></td>
                                    <td><a href=<%='/admin/'+adminId+'/delete/'+a.id+'/'+a.name %>>Delete</a></td>
                                </tr>
                            <%})
                        }%>
                    </tbody>
                    
                </table>
            </div>
        </div>
    </div>

    <script src="/static/js/jquery.js"></script>
    <script src="/static/js/datatables.min.js"></script>
    <script>
        $(document).ready(()=>{
            $('#dtable').DataTable()
        })
    </script>
</body>
</html>